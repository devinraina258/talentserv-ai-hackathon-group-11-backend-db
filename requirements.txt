# Office Leave MCP — Python >=3.10
#
# Runtime only (matches pyproject.toml [project] dependencies):
#   pip install -r requirements.txt
#   pip install -e .
#
# Recommended (editable + tests + graphify), same as README:
#   pip install -e ".[dev,graphify]"
#
# Optional (uncomment or use pip install -e ".[dev,graphify,whatsapp]"):
# pytest>=8.0.0
# graphifyy[mcp]>=0.8.14
# fastapi>=0.115.0
# uvicorn[standard]>=0.32.0
# twilio>=9.0.0
# python-multipart>=0.0.9

fastmcp>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
